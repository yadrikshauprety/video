Installation Steps
git clone https://github.com/yadrikshauprety/video.git
cd video
pip install --upgrade pip
pip install -r requirements.txt
For Backend : Open a new terminal and run python -m uvicorn api:app
For Frontend : Open a new terminal and run python streamlit run app.py
